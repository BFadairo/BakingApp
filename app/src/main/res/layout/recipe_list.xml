<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <android.support.v7.widget.CardView
        android:layout_width="@dimen/recipe_list_card_width"
        android:layout_height="@dimen/recipe_list_card_height"
        android:layout_margin="@dimen/recipe_list_layout_margin"
        android:background="@color/cardview_shadow_start_color">

        <ImageView
            android:id="@+id/recipe_master_image"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:cropToPadding="true"
            android:scaleType="centerCrop"
            android:contentDescription="@string/detail_activity_image_description"
            android:src="@drawable/cheesecake" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/recipe_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="@dimen/recipe_list_text_view_margin"
                android:hint="@string/recipe_name_hint"
                android:textColor="@android:color/white"
                android:textSize="30sp"
                android:textStyle="bold" />
        </LinearLayout>

    </android.support.v7.widget.CardView>

</RelativeLayout>